hazelcast:
  cluster-name: hazelcast-cluster
  network:
    join:
      multicast:
        enabled: false
      tcp-ip:
        enabled: true
        connection-timeout-seconds: 5
        interface: 127.0.0.1
